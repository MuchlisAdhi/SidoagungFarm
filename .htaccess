<IfModule mod_rewrite.c>
    <IfModule mod_negotiation.c>
        Options -MultiViews -Indexes
    </IfModule>

    RewriteEngine On
    
    # Canonical host + HTTPS
    RewriteCond %{HTTP_HOST} ^(?:www\.)?sidoagungfarm\.com$ [NC,OR]
    RewriteCond %{HTTP_HOST} ^(?:career|ftp|hris)\.sidoagungfarm\.com$ [NC]
    RewriteCond %{HTTP_HOST} !^www\.sidoagungfarm\.com$ [NC,OR]
    RewriteCond %{HTTPS} !=on
    RewriteRule ^ https://www.sidoagungfarm.com%{REQUEST_URI} [L,R=301]

    # Handle Authorization Header
    RewriteCond %{HTTP:Authorization} .
    RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]

    # Redirect Trailing Slashes If Not A Folder...
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_URI} (.+)/$
    RewriteRule ^ %1 [L,R=301]

    # Handle Front Controller...
    RewriteCond %{REQUEST_URI} !(\.css|\.js|\.png|\.jpg|\.svg|\.ttf|\.eot|\.woff|\.woff2|\.gif|robots\.txt)$ [NC]
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]

    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(css|js|images|libs|plugins|assets|fonts|dist|docs|bootstrap)/(.*)$ public/$1/$2 [L,NC]

</IfModule>
